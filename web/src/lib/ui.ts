// what a control looks like, so a date input, a filter button and a pager
// button cannot drift apart. padding stays with the caller, a pager button is
// a bigger target than a filter field on purpose
export const CONTROL = 'rounded-lg border border-edge bg-card'
export const FIELD = `${CONTROL} px-2.5 py-1`
